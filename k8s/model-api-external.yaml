apiVersion: v1
kind: Service
metadata:
  name: model-api
  annotations:
    note: "Assumes the host-side model_service.py is reachable at host.docker.internal:8001."
spec:
  type: ExternalName
  externalName: host.docker.internal
  ports:
    - name: http
      port: 8001
      targetPort: 8001
